package com.company;

import java.util.Locale;
import java.util.Scanner;

public class Main {

    public static void main(String[] args) {

        Shape shapeCircle = new Circle();
        shapeCircle.draw();

    /*
        Scanner myObj = new Scanner(System.in);  // Create a Scanner object
        System.out.println("Enter username");
        String userName = myObj.nextLine();  // Read user input

        System.out.println("Choose to enter month in letters? (Y or Yes)");

        String choice = myObj.nextLine();

        System.out.println(choice.toUpperCase());

        if (choice.toUpperCase(Locale.ROOT).equals("Y") || choice.toUpperCase(Locale.ROOT).equals("YES")){
            System.out.println("Enter month (letters only)");
            String month = myObj.nextLine();  // Read user input
            int numericMonth = SelectedMonthType.stringMethod(month);
            System.out.println("Username is: " + userName);  // Output user input
            System.out.println("Selected month is: " + month + " which is the number " + numericMonth + " month");  // Output user input

        } else {
            System.out.println("Enter month (numbers only (1-12))");
            int month = myObj.nextInt();  // Read user input
            String stringMonth = SelectedMonthType.callIntMethod(month);
            System.out.println("Username is: " + userName);  // Output user input
            System.out.println("Selected number of month is: " + month + " which the " + stringMonth);  // Output user input
        }
        */

    }
}


